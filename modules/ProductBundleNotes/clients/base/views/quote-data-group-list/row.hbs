{{!--
/*
 * Your installation or use of this SugarCRM file is subject to the applicable
 * terms available at
 * http://support.sugarcrm.com/Resources/Master_Subscription_Agreements/.
 * If you do not agree to all of the applicable terms or do not have the
 * authority to bind the entity as an authorized representative, then do not
 * install or use this SugarCRM file.
 *
 * Copyright (C) SugarCRM Inc. All rights reserved.
 */
--}}
<tr name="{{model.module}}_{{model.cid}}" data-group-id="{{groupId}}" record-id="{{model.id}}"
    class="quote-data-group-list product-bundle-notes-row ltr:[&>td:not(:first-child)]:left-[72px] rtl:[&>td:not(:first-child)]:right-[72px] {{#if model.id}} sortable ui-sortable{{else}} not-sortable{{/if}}">
    <td class="absolute bg-[--foreground-base] box-border description-textarea-buttons focus-within:z-30 sticky-border-tl z-10">
        <span class="absolute action-button-wrapper inset-0 left-column-save-cancel p-2">
            {{#each view.leftSaveCancelColumn}}
                {{field ../view model=../model template=../view.viewName}}
            {{/each}}
        </span>
        <span class="absolute action-button-wrapper inset-0 left-column-buttons px-2 py-1.5">
            {{#each view.leftColumns}}
                {{field ../view model=../model template=../view.viewName}}
            {{/each}}
        </span>
    </td>
    {{#each model.fields}}
        {{#eq name 'description'}}
            <td class="description-textarea relative" colspan="{{../../view.listColSpan}}">
                {{field ../../view model=../../model template=../../view.viewName}}
            </td>
        {{/eq}}
    {{/each}}
</tr>
